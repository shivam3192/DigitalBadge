<h2 class="page-header">Dashboard</h2>

<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>

<script language="Javascript" type="text/javascript">
   $.ajax({
                  url: "/state/aarushi",
                  // Handle as Text
                  dataType: "text",
                  success: function(data) {
                      // Parse JSON file
                      var json = $.parseJSON(data);


                      //Store data into a variable
                      // Display Players
                      for(var i=1;i<=Number(json.value)+1;i++ )
                      $("#"+i).prop('disabled', false);
                       console.log("here"+Number(json.value));
                     // $('#results').html('Currently there are: ' + json.value ' users');
                  }
      });









</script>
<p>Welcome to your dashboard</p>  
<br>
<form action="/transactions" method="post">
	<div class="form-group">
	   <label>Cleared </label>
	<Button type="submit" disabled name="submit" value="1" class="btn btn-default" id="1">Level1</Button>	
<br>
<br>
	   <label>Cleared </label>
	  <Button type="submit" disabled value="2" name="submit" class="btn btn-default" id="2">Level2</Button>
	   <label>Cleared </label>
	  <Button type="submit" disabled value="3" name="submit" class="btn btn-default" id="3">Level3</Button>
	  </div>
</form>
	
